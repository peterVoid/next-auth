import FormLogin from "@/components/FormLogin";

function LoginPage() {
  return (
    <div className="flex items-center justify-center h-full bg-gradient-to-r from-blue-500 to-indigo-500">
      <FormLogin />
    </div>
  );
}

export default LoginPage;
